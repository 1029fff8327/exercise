export enum RedisConstants {
  options = 'module_redis_options_token',
  client = 'module_redis_client_token',
  refreshTokenPrefix = 'refresh_token:',
  resetTokenPrefix = 'reset_token:',
  accessTokenPrefix = 'access_token:',
  activationTokenPrefix = 'activation_token:',
}
